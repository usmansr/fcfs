#include <stdio.h>
#include <unistd.h>
#include <sys/stat.h>
#include <fnctl.h>

int main(void)
{
    int n, fd;
    char buffer[50];

    fd = open("usman.txt", O_CREAT | O_RDWR, 0777);

#include <stdio.h>
#include <unistd.h>

int main(void)
{
    int n, fd;
    char* buffer;
    printf("Enter Text: \n");
    n = read(0, buffer, 50);
    write(1, buffer, n);

    return 0;
}

    printf("Enter Text: \n");
    n = read(0, buffer, 50);
    write(1, buffer, n);

    return 0;
}
